{
  "api_base_url": {
    "description": "图床 API 基础地址",
    "type": "string",
    "default": "https://img.scdn.io/api/v1.php",
    "hint": "scdn 图床 API 端点，默认 https://img.scdn.io/api/v1.php"
  },
  "default_cdn_domain": {
    "description": "默认 CDN 域名",
    "type": "string",
    "default": "img.scdn.io",
    "hint": "可选：img.scdn.io / cloudflareimg.cdn.sn / edgeoneimg.cdn.sn / esaimg.cdn1.vip / cloudflarecnimg.scdn.io / anycastimg.scdn.io / edgeoneimg.cdn1.vip（完整列表以 main.py _SCDN_DOMAINS 为准，见 scripts/check_sync.py）"
  },
  "default_storage": {
    "description": "默认存储位置",
    "type": "string",
    "default": "local",
    "options": ["local", "telegram", "r2"],
    "hint": "local=默认, telegram=Telegram, r2=Cloudflare R2"
  },
  "default_output_format": {
    "description": "默认输出格式",
    "type": "string",
    "default": "auto",
    "options": ["auto", "jpg", "jpeg", "png", "webp", "gif", "webp_animated"],
    "hint": "auto 为服务端默认压缩转 webp"
  },
  "timeout": {
    "description": "请求超时时间（秒）",
    "type": "int",
    "default": 120,
    "slider": {"min": 10, "max": 300, "step": 10},
    "hint": "上传大图片或视频时可能需要更长时间"
  }
}
